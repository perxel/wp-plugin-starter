# Ignore vendor/ contents, but keep the committed shared admin-UI kit.
# (Must exclude the entries, not the dir, or the negation can't re-include.)
/vendor/*
!/vendor/perxel-ui/
/node_modules/
composer.lock
package-lock.json
.DS_Store
*.log
/.idea/
/dist/
